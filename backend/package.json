{
  "name": "portfolio-ib-backend",
  "version": "1.0.0",
  "description": "Backend for Ibrahim Shamshad portfolio — video upload via Cloudinary",
  "main": "server.js",
  "scripts": {
    "start": "node --use-system-ca server.js",
    "dev": "nodemon --exec \"node --use-system-ca\" server.js",
    "test": "node --test tests/*.test.js",
    "seed:media": "node --use-system-ca scripts/seedMedia.js"
  },
  "dependencies": {
    "cloudinary": "^2.5.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "streamifier": "^0.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
